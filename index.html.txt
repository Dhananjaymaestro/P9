<!DOCTYPE html>
<html lang="hi">
<head>
<meta charset="UTF-8">
<title>SSP Poster - Ratna</title>

<style>
    @media print {
        body {
            margin: 0;
            padding: 0;
        }
        .poster {
            width: 297mm; /* A3 size width */
            height: 420mm; /* A3 size height */
            border: 0;
            box-shadow: none;
            page-break-inside: avoid;
        }
    }

    body {
        margin: 0;
        padding: 0;
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        background: #ffffff;
        display: flex;
        justify-content: center;
        align-items: center;
        min-height: 100vh;
    }

    .poster {
        background: #ffffff;
        border: 8px solid #0073e6;
        border-radius: 30px;
        padding: 60px;
        width: 780px;
        text-align: center; /* Centre aligned */
        box-shadow: 0 20px 40px rgba(0,0,0,0.3);
        position: relative;
        overflow: hidden;
    }

    /* Logo Style */
    .logo {
        position: absolute;
        top: 30px;
        left: 30px;
        width: 80px;
        height: 80px;
        background: linear-gradient(135deg, #0073e6, #00aaff);
        border-radius: 50%;
        display: flex;
        justify-content: center;
        align-items: center;
        font-weight: bold;
        color: #ffffff;
        font-size: 20px;
        box-shadow: 0 5px 15px rgba(0,0,0,0.2);
    }

    .poster h1 {
        background: linear-gradient(90deg, #0073e6, #00aaff);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        font-size: 55px;
        margin-bottom: 20px;
        font-weight: bold;
        text-shadow: 2px 2px 5px rgba(0,0,0,0.2);
    }

    .poster h2 {
        background: linear-gradient(90deg, #004080, #0066cc);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        font-size: 30px;
        margin: 15px 0;
        font-weight: semi-bold;
        text-shadow: 1px 1px 3px rgba(0,0,0,0.2);
    }

    .poster p {
        color: #333333;
        font-size: 21px;
        margin: 12px 0;
        line-height: 1.5;
    }

    .poster .slogan {
        background: linear-gradient(90deg, #0059b3, #3399ff);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        font-size: 24px;
        font-weight: bold;
        margin: 25px 0;
        text-shadow: 1px 1px 4px rgba(0,0,0,0.2);
    }

    /* Founder style updated */
    .poster .founder {
        position: absolute;
        bottom: 5px; /* Aur niche shift kiya */
        left: 50%;
        transform: translateX(-50%);
        color: #004080;
        font-size: 24px; /* Prominent */
        font-weight: bold;
        font-style: normal;
        text-shadow: 1px 1px 3px rgba(0,0,0,0.2);
    }
</style>
</head>

<body>
<div class="poster">
    <div class="logo">SSP</div>
    <h1>रत्ना</h1>
    <h2>समाज सुधारी पार्टी (SSP)</h2>
    <p class="slogan">पैसा नहीं, प्यार लाएंगे, हर जगह सुधार लाएंगे।</p>
    <p>संस्कारी बनो → शिक्षित बनो → आंदोलन करो → संगठित हो।</p>
    <p class="founder">संस्थापक: धनंजय</p>
</div>
</body>
</html>